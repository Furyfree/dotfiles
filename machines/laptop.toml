# Nimbus machine manifest for the laptop workstation.
# Nimbus owns this file; Chezmoi owns the surrounding checkout and Git.
# Components are added once the component catalog exists in Nimbus.
schema = 1
profiles = ["common", "development", "hyprland-noctalia"]
components = []
packages = []
package_exclusions = []

[dotfiles]
repo = "https://github.com/Furyfree/dotfiles.git"
