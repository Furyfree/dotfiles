{
    "layer": "top",
    "position": "top",

    "modules-left": ["hyprland/workspaces"],
    "modules-center": ["custom/clock"],
    "modules-right": ["network", "pulseaudio", "battery", "tray", "custom/lock", "custom/power"],

    "hyprland/workspaces": {
        "disable-scroll": true,
        "sort-by-name": true,
        "format": " {name} ",
        "format-icons": {
        "default": ""
        }
    },

    "clock": {
    "format": " {:%H:%M}",
    "tooltip-format": "<big>{:%A, %d %B %Y}</big>\n<tt><small>{calendar}</small></tt>",
    "format-alt": " {:%d/%m/%Y}"
    },

    "network": {
        "format-wifi": "  {essid} ({signalStrength}%)",
        "format-ethernet": "  {ifname}",
        "tooltip-format": "{ipaddr}/{cidr}",
        "format-disconnected": "󰖪  Disconnected",
        "on-click": "nm-connection-editor"
    },

    "pulseaudio": {
        "format": "{icon} {volume}%",
        "format-muted": "",
        "format-icons": {
        "default": ["", "", ""]
        },
        "on-click": "pavucontrol"
    },

    "battery": {
        "format": "{icon} {capacity}%",
        "format-charging": " {capacity}%",
        "format-plugged": " {capacity}%",
        "format-icons": ["", "", "", "", ""],
        "states": {
            "warning": 30,
            "critical": 15
        },
        "on-click": "xfce4-power-manager-settings"
},

    "custom/clock": {
        "format": " {}",
        "interval": 60,
        "exec": "date '+%H:%M   %d/%m/%Y'",
        "tooltip": true,
        "tooltip-format": "<tt>Full: {}</tt>"
    },

    "tray": {
        "icon-size": 21,
        "spacing": 10
    },

    "custom/lock": {
        "tooltip": false,
        "on-click": "hyprlock",
        "format": ""
    },

    "custom/power": {
        "tooltip": false,
        "on-click": "wlogout &",
        "format": "⏻"
    }
}
