{{- $profiles := list "common" -}}
{{- if hasKey . "profiles" -}}
{{-   $profiles = .profiles -}}
{{- end -}}
{{- $hyprlandNoctalia := has "hyprland-noctalia" $profiles -}}
{{- /* Niri and DMS profiles are disabled during the initial setup. */ -}}

{{- if not $hyprlandNoctalia -}}
.config/hypr/**
.config/noctalia/**
{{ end -}}
.config/niri/**
.config/DankMaterialShell/**

# The broad .config rule below supersedes these profile rules while scaffolding.
# Empty scaffold. Narrow these directory rules as configs are implemented.
.config/**
.local/**
.ssh/**
AppData/**
Documents/**
Library/**

.gitconfig
.gitignore
.zshenv
.profile
.bash_profile
.bashrc
.bash_logout
.blerc
