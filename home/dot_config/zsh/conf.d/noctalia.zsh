# Noctalia generates this file after its first palette render.
if [ -r "${XDG_CONFIG_HOME:-$HOME/.config}/fzf/themes/noctalia.sh" ]; then
    . "${XDG_CONFIG_HOME:-$HOME/.config}/fzf/themes/noctalia.sh"
fi
