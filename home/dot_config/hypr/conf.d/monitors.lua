-- Monitor modes, scaling, positions, and output-specific settings.
-- Keep automatic settings until both monitor setups are known; template only if they differ.
hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto",
})
