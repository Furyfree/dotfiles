#!/usr/bin/env bash
set -euo pipefail
shopt -s nullglob
latest=$(ls -1dt "$HOME/.local/share/JetBrains/Toolbox/apps"/*rider*/bin/rider 2>/dev/null | head -n1)
[[ -x "${latest:-}" ]] && exec "$latest" "$@"
echo "Rider launcher not found." >&2; exit 1
