#!/usr/bin/env bash
set -euo pipefail
shopt -s nullglob
latest=$(ls -1dt "$HOME/.local/share/JetBrains/Toolbox/apps"/*idea*/bin/idea 2>/dev/null | head -n1)
[[ -x "${latest:-}" ]] && exec "$latest" "$@"
echo "IntelliJ launcher not found." >&2; exit 1
